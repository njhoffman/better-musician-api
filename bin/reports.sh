#!/bin/sh

export NODE_ENV=test && node ./node_modules/karma/bin/karma start config/karma.config
plato -r -x "interfaces" -d reports/plato/current src

# every git commit will generate reports to reports/current for flow, lint, karma, and plato
# after all current reports are generated, it will take summary data or re-run for json output to be put into history folder
# custom summary html file generated by me showing history of interesting metrics
# pushes to server will (if correct host identified) trigger the browser tests
